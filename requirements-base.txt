# ARGUS shared runtime requirements.
#
# These packages are common to API, inference, and training environments.
# PyTorch and torchvision are intentionally omitted:
# install them first from the platform-specific wheel index for the host.

# Core astronomy
astropy==6.1.0
photutils==1.13.0
sep==1.2.1
astride==0.3.2

# Orbital mechanics
sgp4==2.23
skyfield==1.49
spacetrack==1.4.0

# Image processing
# Headless variant: safe in WSL2 and server environments.
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
Pillow==12.2.0
matplotlib==3.9.0  # imported by ASTRiDE

# Data / compute
# numpy MUST stay < 2.0: sep and astride are compiled against numpy 1.x ABI.
numpy==1.26.4
scipy==1.13.1

# Utilities
requests==2.33.0
