# ARGUS Claude Code Entry Point

Claude Code: read `agent_docs/assistant_guide.md` before starting work in this
repo. That file is the shared source of truth for ARGUS assistant instructions.

Keep this file intentionally small. Codex uses `AGENTS.md`, Claude Code uses
`CLAUDE.md`, and both point at the same canonical guide.
