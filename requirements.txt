# ARGUS full local development requirements.
#
# This compatibility aggregate keeps the historical `pip install -r
# requirements.txt` path working for local development. For cleaner, smaller
# environments prefer the explicit lanes:
#
#   requirements-api.txt        API service, no ML stack
#   requirements-inference.txt  model-serving worker extras, no training extras
#   requirements-training.txt   training/evaluation extras
#   requirements-dev.txt        tests on top of training
#
# PyTorch and torchvision are intentionally omitted from
# all requirements files because they require platform-specific wheel indexes.
# Install them first using the appropriate wheel index for the host.

-r requirements-dev.txt
