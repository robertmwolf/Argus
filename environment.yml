# Tested macOS ARGUS development environment. See README.md for the manual
# installation path when a platform-specific CUDA build of PyTorch is needed.
name: satid
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements-dev.txt
