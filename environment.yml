# Minimal ARGUS conda environment. PyTorch is platform-specific and should be
# installed before the pip requirements described in README.md.
name: satid
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements-dev.txt
