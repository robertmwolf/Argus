# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/

# Virtual environments / conda
.env
.venv
env/
venv/
ENV/

# Data — never check in raw FITS or large archives
data/milan/
data/frigate/
data/cache/
data/GTImages/*.fits
data/GTImages/*.fit
data/GTImages/*.fts
*.fits
*.fit
*.fts
*.zip

# Keep sample placeholder
!data/sample/.gitkeep

# Results — check in JSON metrics, not raw outputs
results/*.png
results/*.csv
# Large raw prediction/proposal dumps (regenerable, too big for git)
results/*/proposals*.json
results/**/proposals*.json
results/*/predictions.json
results/**/*.bbox.json
# External-drive symlinks (not portable)
data/BrentImages
data/BrentImages/

# Credentials — never commit
.env.local
.envrc
secrets.json

# Claude Code local settings (machine-specific paths)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Logs
*.log

# Model weights — never commit large binaries
weights/
*.pth
*.pt
*.ckpt

# Training logs and checkpoints
training/logs/
training/work_dirs/

# SQLite dev database
*.db
*.db-shm
*.db-wal
argus*.db
argus.db-*

# Docker uploads volume (local dev)
data/uploads/

# TLE source data — large archives, never commit
data/tle_zips/

# Eval per-image prediction dumps (large, regenerated by benchmark)
eval/results/*.json

# Training data — lives on external drive, accessed via symlinks
# Trailing-slash patterns match directories; bare names also match symlinks.
data/satstreaks
data/satstreaks/
data/dev_subset
data/dev_subset/
data/raw/
data/processed/
data/augmented
data/augmented/
data/gtimages_synthetic
data/gtimages_synthetic/
data/annotations
data/annotations/

weights/yolo_baseline/
weights/local_run/
weights/run/
runs/
